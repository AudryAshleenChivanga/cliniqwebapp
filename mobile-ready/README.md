# Mobile Ready (PWA)

ClinIQ mobile readiness is implemented in `frontend/` using:
- Service worker: `frontend/public/sw.js`
- Manifest: `frontend/public/manifest.webmanifest`
- Offline queue: `frontend/lib/offline.ts`

Install the web app on Android/iOS from browser menu for app-like operation.
